# React/Vite Frontend Specific
# Main .gitignore is at smartpark/.gitignore

# Build outputs
dist/
build/
*.local

# Dependencies (if not covered by root)
node_modules/

# Vite specific
.vite/

# Environment files (redundant with root but kept for clarity)
.env
.env.local
.env.production.local

# Editor files (if needed locally)
.vscode/settings.json
